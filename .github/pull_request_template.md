<!-- Conventional Commits title, e.g. feat: add openmetadata_data_product -->

## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves. Skip for typo fixes. -->

## Checklist

- [ ] `make test` and `make lint` pass
- [ ] Acceptance tests pass (`make testacc`), or CI has run them
- [ ] New or changed schema: `make docs` re-run, `docs/` committed, and `examples/` updated
- [ ] New resource: registered in `provider.go` and has an acceptance test
- [ ] Squashed to one commit
